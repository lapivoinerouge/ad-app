import { Routes, Route } from 'react-router-dom';

const App = () => {
  return (
    <main>
      <Routes>
        {/* <Route path="/" element={<Home />} />
        <Route path='*' element={<ErrorPage />} /> */}
      </Routes>
   </main>
  );
}

export default App;